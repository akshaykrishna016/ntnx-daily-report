"""Render package: chart generation, HTML rendering and CSV output.

Modules:
    charts  -- matplotlib PNG chart generation returned as base64 data URIs.
    html    -- jinja2 rendering of the self-contained report and the email body.
    csvout  -- full VM inventory CSV writer.
"""
